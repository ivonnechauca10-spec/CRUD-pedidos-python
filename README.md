# CRUD Pedidos Python 📝

Sistema CRUD (Crear, Leer, Actualizar, Borrar) para gestionar pedidos usando Python.  
Incluye un archivo `.bat` para ejecutar automáticamente el programa en Windows.

## Archivos del proyecto

- `programa.py` → Código principal del sistema.  
- `ejecutar.bat` → Ejecuta el programa automáticamente en Windows.  
- `README.md` → Este archivo.

## Cómo ejecutar

### Windows (recomendado)
Haz doble clic en `ejecutar.bat` para iniciar el programa.

### Usando Python
Abre la terminal en la carpeta del proyecto y ejecuta:

```bash
python programa.py
## Funcionalidades

- Crear, listar, actualizar y eliminar pedidos.  
- Automatización mediante `.bat` en Windows.

## Requisitos

- Python 3.x
- Windows (si usas `.bat`)

